{
  "name": "plinto",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "predev": "pnpm --filter @plinto/shared build",
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
